@Card(id: "db-intro-dbms-card-pipeline", revision: 1, sourcePage: "db-intro-dbms-page-pipeline", topic: "数据库基础", accent: "mint", frontTitle: "DBMS 一条指令", frontSubtitle: "检查之后才落盘", backTitle: "管道") {
接收指令 → 检查规则 → 执行读写 → 返回结果。不合格写入通常整笔拒绝。

@Highlight {
先挡错，再落盘。
}
}
