@Tip(id: "db-intro-dbms-tip-reject", revision: 1, sourcePage: "db-intro-dbms-page-pipeline", title: "不合格的写入常被整笔拒绝", systemImage: "lightbulb", accent: "mint") {
规则检查通常发生在写入前：空电话之类的错误，往往进不了库，而不是先存再删。
}
