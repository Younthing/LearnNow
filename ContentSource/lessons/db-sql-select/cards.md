@Card(id: "db-sql-select-card", revision: 1, sourcePage: "db-sql-select-page-basic", topic: "数据库基础", accent: "mint", frontTitle: "SELECT", frontSubtitle: "会不会改表", backTitle: "只读") {
SELECT 从原表投影出结果集；通常不修改原表行或列结构。

@Highlight {
查出两列 ≠ 删掉其它列。
}
}
