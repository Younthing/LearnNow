@Card(id: "db-rel-dup-card", revision: 1, sourcePage: "db-rel-dup-page-grain", topic: "数据库基础", accent: "mint", frontTitle: "连接后的重复", frontSubtitle: "阿明出现三次", backTitle: "看粒度") {
一对多连接时，「一」侧字段会随每条「多」侧行复制。若要人的名单，需改粒度或去重。

@Highlight {
先问结果一行代表订单还是代表会员。
}
}
