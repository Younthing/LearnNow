@Card(id: "db-query-where-card", revision: 1, sourcePage: "db-query-where-page-filter", topic: "数据库基础", accent: "mint", frontTitle: "WHERE", frontSubtitle: "列还是行", backTitle: "筛行") {
WHERE 按条件决定哪些行进入结果；SELECT 列表决定哪些列出现。

@Highlight {
投影管列，WHERE 管行。
}
}
