@Card(id: "py-args-ret-card-args", revision: 1, sourcePage: "py-args-ret-page-args", topic: "函数与程序组织", accent: "mint", frontTitle: "参数负责什么", frontSubtitle: "函数接口", backTitle: "把数据送进函数") {
调用括号里的值送入参数。同一套步骤因此能处理不同输入。

@Highlight {
参数是入口。
}
}

@Card(id: "py-args-ret-card-ret", revision: 1, sourcePage: "py-args-ret-page-ret", topic: "函数与程序组织", accent: "mint", frontTitle: "print 能代替 return 吗", frontSubtitle: "返回值", backTitle: "不能，职责不同") {
print 把信息送到屏幕；return 把值交回调用处，才能继续赋值或判断。

@Highlight {
给人看用 print；给程序用 return。
}
}
