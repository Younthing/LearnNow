@Card(id: "db-sql-update-card", revision: 1, sourcePage: "db-sql-update-page-set", topic: "数据库基础", accent: "mint", frontTitle: "UPDATE 与 WHERE", frontSubtitle: "忘了会怎样", backTitle: "锁定行") {
UPDATE 修改已有行的列值；WHERE 决定改谁。没有 WHERE 可能改整表。

@Highlight {
先 SELECT 同一条件，再 UPDATE。
}
}
