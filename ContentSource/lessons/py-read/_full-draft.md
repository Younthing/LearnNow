# 完整讲义稿（存档，不编译）

## Brief（写作合同）

| 项 | 内容 |
|----|------|
| **核心问题** | 怎样读取文本文件？ |
| **心智模型** | 打开文件 → 读出文本 → 用完关闭（with 自动关）。 |
| **学习者起点** | 会字符串处理。课内建立：路径、read/按行、with。 |
| **终点行为** | 1. 能说明读文件是把磁盘文字载入程序。2. 能按行处理。3. 能说出 with 负责关闭。 |
| **概念预算** | 核心：读入文本。支撑：open、逐行、with。识别：编码。延后：二进制、大文件策略。 |
| **贯穿例子** | study.txt 每天一行「科目,分钟」。 |
| **范围边界** | 会讲：文本读取流程。不讲：异常细节（后课）、写文件。 |

## 讲义

分钟记在 study.txt 里。程序要先打开文件，读出文字，再按行拆。

用 with open(...) as f 读取，用完自动关闭。读到的是字符串，再交给上一课的 split。

## 拆页

| 页 | 任务 |
|----|------|
| open-read | 打开并读出文本 |
| lines | 按行处理 |
