@Card(id: "cs-mem-where-card-mem", revision: 1, sourcePage: "cs-mem-where-page-mem", topic: "内存", accent: "mint", frontTitle: "运行中数据主要在哪", frontSubtitle: "存放", backTitle: "内存工作台") {
供 CPU 快速读写。

@Highlight {
正在用的变量与数组，先想内存。
}
}

@Card(id: "cs-mem-where-card-persist", revision: 1, sourcePage: "cs-mem-where-page-persist", topic: "持久化", accent: "amber", frontTitle: "只放内存够不够长期保存", frontSubtitle: "断电/结束后", backTitle: "不够；需要持久存储") {
工作台会清。

@Highlight {
跨会话的数据要另存。
}
}
