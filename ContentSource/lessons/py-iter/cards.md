@Card(id: "py-iter-card-acc", revision: 1, sourcePage: "py-iter-page-agg", topic: "常用数据结构", accent: "mint", frontTitle: "合计变量放哪", frontSubtitle: "遍历处理", backTitle: "放在循环外，每轮更新") {
累加器在 for 外初始化，在循环体内更新。放进体内开头会每轮清零。

@Highlight {
遍历是轨道，处理是货物。
}
}

@Card(id: "py-iter-card-filter", revision: 1, sourcePage: "py-iter-page-filter", topic: "常用数据结构", accent: "mint", frontTitle: "过滤和转换差在哪", frontSubtitle: "处理模式", backTitle: "收不收 vs 变成什么") {
过滤决定是否留下原值；转换把每个元素变成新值（如布尔）。

@Highlight {
每轮做一件清楚的事。
}
}
