@Card(id: "db-rel-join-card", revision: 1, sourcePage: "db-rel-join-page-inner", topic: "数据库基础", accent: "mint", frontTitle: "JOIN", frontSubtitle: "凭什么拼在一起", backTitle: "对齐条件") {
JOIN 按条件（通常是主键=外键）把多表行拼成宽结果；内连接要求匹配成功。

@Highlight {
连接是读法，不是把表永久糊回去。
}
}
