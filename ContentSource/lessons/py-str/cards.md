@Card(id: "py-str-card-concat", revision: 1, sourcePage: "py-str-page-build", topic: "文本与文件", accent: "mint", frontTitle: "数字怎样拼进句子", frontSubtitle: "字符串", backTitle: "先转成文本再 +") {
用 str(数字) 转成字符串后，才能和文字用 + 拼接。

@Highlight {
拼接的是文本零件。
}
}

@Card(id: "py-str-card-split", revision: 1, sourcePage: "py-str-page-split", topic: "文本与文件", accent: "mint", frontTitle: "一行「语文,40」怎么拆", frontSubtitle: "切割清理", backTitle: "split 再 strip") {
按逗号 split 成零件，对科目名 strip 后再比较或当键。

@Highlight {
空白也是字符，比较前先清理。
}
}
