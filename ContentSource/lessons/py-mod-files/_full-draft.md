# 完整讲义稿（存档，不编译）

## Brief（写作合同）

| 项 | 内容 |
|----|------|
| **核心问题** | 怎样把程序拆成多个文件？ |
| **心智模型** | 一个 .py 可当模块；用 import 取用其中的函数。 |
| **学习者起点** | 会函数。课内建立：模块、import、职责分开。 |
| **终点行为** | 1. 能说明为何拆文件。2. 能写出 import 使用函数。3. 能判断工具函数与主流程分开放。 |
| **概念预算** | 核心：文件即模块。支撑：import、命名空间、主文件。识别：from import。延后：包、路径、pip。 |
| **贯穿例子** | study_lib.py 提供 is_done，main 里调用。 |
| **范围边界** | 会讲：拆文件与 import。不讲：包管理、第三方安装。 |

## 讲义

一个文件几千行时，找函数很累。把「判断达标」放进 study_lib.py，主文件 import 后再用。

拆分按职责：库文件提供工具，主文件编排流程。

## 拆页

| 页 | 任务 |
|----|------|
| why-split | 为何拆成多文件 |
| import | 用 import 取用另一文件的函数 |
