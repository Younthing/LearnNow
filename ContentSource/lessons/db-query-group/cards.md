@Card(id: "db-query-group-card", revision: 1, sourcePage: "db-query-group-page-bucket", topic: "数据库基础", accent: "mint", frontTitle: "GROUP BY", frontSubtitle: "一行代表谁", backTitle: "一组摘要") {
GROUP BY 按键把行收组；结果一行通常是一个组的统计摘要。

@Highlight {
明细粒度 → 汇总粒度。
}
}
