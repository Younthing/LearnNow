@Card(id: "web-server-get-post-card-diff", revision: 1, sourcePage: "web-server-get-post-page-diff", topic: "服务器端程序", accent: "mint", frontTitle: "GET 和 POST", frontSubtitle: "取回与提交", backTitle: "意图不同") {
GET 侧重取回查看；POST 侧重提交并处理。

@Highlight {
名字背后是意图，不只是拼写差别。
}
}

@Card(id: "web-server-get-post-card-choose", revision: 1, sourcePage: "web-server-get-post-page-choose", topic: "服务器端程序", accent: "mint", frontTitle: "怎么选", frontSubtitle: "看列表还是发布", backTitle: "先问意图") {
打开查看多用 GET；发布创建多用 POST。

@Highlight {
按意图选方法，避免把正文塞进 URL。
}
}
